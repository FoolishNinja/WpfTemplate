<UserControl x:Class="WpfTemplate.Controls.DropDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTemplate.Controls"
             mc:Ignorable="d" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid>
        <ComboBox Name="DropDownComboBox" SelectionChanged="DropDownComboBox_SelectionChanged" Text="{Binding Placeholder}" IsEditable="True" IsReadOnly="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FlowDirection="LeftToRight">
        </ComboBox>
    </Grid>
</UserControl>

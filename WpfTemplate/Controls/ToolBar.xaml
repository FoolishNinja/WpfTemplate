<UserControl x:Class="WpfTemplate.Controls.ToolBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTemplate.Controls"
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid>
        <ToolBarTray Background="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ToolBar Background="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Menu Background="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Name="Menu"/> 
            </ToolBar>
        </ToolBarTray>
    </Grid>
</UserControl>
